networks:
  infra-net:
    external: true
    name: infra-net

services:
  agent:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    networks:
      - infra-net
    volumes:
      - ./credentials.json:/app/credentials.json:ro
      - ./token.json:/app/token.json
